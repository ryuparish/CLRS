In order to create a heap from a list of n elements, I would have to split the elements into groups that increase consecutively based on 2^x.

Each group can be thought of as a layer in the heap, meaning that the height of the heap (a count of all the layers) would be log_2(elements).
